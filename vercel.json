{
  "version": 2,
  "builds": [
    {
      "src": "app/app.py",  // Path to app.py (since it's inside the 'app' folder)
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/app/app.py"  // Same path here
    }
  ]
}
